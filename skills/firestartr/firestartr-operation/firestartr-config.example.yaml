# Runtime, git-ignored. Copy to firestartr-config.yaml and fill in.
# Schema: reference/config-schema.md

organizations:
  - name: "prefapp-demo"
    paths:
      - "~/work/prefapp-demo"
      # - "~/work/prefapp-demo-fork"

    # claims_repo: "prefapp-demo/claims"
    # state_github_repo: "prefapp-demo/state-github"
    # state_infra_repo: "prefapp-demo/state-infra"
    # catalog_repo: "prefapp-demo/catalog"

  # - name: "another-org"
  #   paths:
  #     - "~/work/another-org"

# cli_version: "0.1.0"
